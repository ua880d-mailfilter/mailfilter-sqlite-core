#ifndef LEGACY_UTILS_HH
#define LEGACY_UTILS_HH

#include <string>

int cmp_no_case(const std::string&, const std::string&);
std::string int_to_string(int);
std::string exec_shell(const char*);

#endif
